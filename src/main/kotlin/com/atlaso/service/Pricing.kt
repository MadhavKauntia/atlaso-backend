package com.atlaso.service

/** Central pricing constants shared across order and coupon logic. */
object Pricing {
    /** Flat all-inclusive unit price in paise (Rs. 2499). */
    const val UNIT_PRICE_MINOR = 249900L
}
